mkdocs>=1.6
mkdocs-material>=9.5
mkdocs-awesome-pages-plugin>=2.9  # 让 nav 从文件结构 + .pages 文件自动派生
jieba>=0.42  # 中文搜索分词
mdx_truly_sane_lists>=1.3  # 让 2 空格缩进就能识别嵌套 bullet（GFM 风格，而不是 Python-Markdown 默认的 4 空格）
